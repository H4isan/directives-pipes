import { DirectiveEventDirective } from './directive-event.directive';

describe('DirectiveEventDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveEventDirective();
    expect(directive).toBeTruthy();
  });
});
