import { PpremplacePipe } from './ppremplace.pipe';

describe('PpremplacePipe', () => {
  it('create an instance', () => {
    const pipe = new PpremplacePipe();
    expect(pipe).toBeTruthy();
  });
});
